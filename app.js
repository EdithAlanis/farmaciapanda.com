const cfg=window.FARMACIA_PANDA_CONFIG||{};const products=(window.PRODUCTS||[]).filter(p=>p&&p.name);let cart=JSON.parse(localStorage.getItem('panda-cart')||'{}');const $=s=>document.querySelector(s);const money=n=>new Intl.NumberFormat('es-MX',{style:'currency',currency:'MXN'}).format(Number(n||0));
function categories(){const vals=[...new Set(products.map(p=>p.category).filter(Boolean))].sort();$('#category').innerHTML='<option value="Todos">Todas las categorías</option>'+vals.map(v=>`<option>${v}</option>`).join('')}
function render(){const q=$('#search').value.toLowerCase().trim(),c=$('#category').value;const list=products.filter(p=>(c==='Todos'||p.category===c)&&(`${p.name} ${p.detail||''}`.toLowerCase().includes(q)));$('#productGrid').innerHTML=list.map(p=>`<article class="product ${p.special?'special':''}"><span class="icon">${p.icon||'💊'}</span><h3>${p.name}</h3><p>${p.detail||p.category||''}</p><strong>${p.price?money(p.price):'Consultar precio'}</strong><button data-add="${p.id}">Agregar al carrito</button></article>`).join('')||'<p>No se encontraron productos.</p>';document.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>add(b.dataset.add))}
function add(id){cart[id]=(cart[id]||0)+1;save();openCart()};function save(){localStorage.setItem('panda-cart',JSON.stringify(cart));renderCart()};function renderCart(){const rows=Object.entries(cart).map(([id,q])=>[products.find(p=>p.id===id),q]).filter(x=>x[0]);$('#cartCount').textContent=rows.reduce((a,x)=>a+x[1],0);$('#cartItems').innerHTML=rows.map(([p,q])=>`<div class="cart-row"><div><b>${p.name}</b><br><small>${money(p.price)} × ${q}</small></div><div class="qty"><button data-minus="${p.id}">−</button> ${q} <button data-plus="${p.id}">+</button></div></div>`).join('')||'<p>Tu carrito está vacío.</p>';$('#subtotal').textContent=money(rows.reduce((a,[p,q])=>a+p.price*q,0));document.querySelectorAll('[data-minus]').forEach(b=>b.onclick=()=>{cart[b.dataset.minus]--;if(cart[b.dataset.minus]<=0)delete cart[b.dataset.minus];save()});document.querySelectorAll('[data-plus]').forEach(b=>b.onclick=()=>{cart[b.dataset.plus]++;save()})}
function openCart(){$('#drawer').classList.add('open');$('#drawer').setAttribute('aria-hidden','false');$('#overlay').hidden=false}function closeCart(){$('#drawer').classList.remove('open');$('#overlay').hidden=true}
$('#cartBtn').onclick=openCart;$('#closeCart').onclick=closeCart;$('#overlay').onclick=closeCart;$('#search').oninput=render;$('#searchBtn').onclick=render;$('#category').onchange=render;$('#orderBtn').onclick=()=>{if(!Object.keys(cart).length)return alert('Agrega productos al carrito.');closeCart();$('#checkout').hidden=false};$('#closeCheckout').onclick=()=>$('#checkout').hidden=true;
$('#orderForm').onsubmit=async e=>{e.preventDefault();const fd=Object.fromEntries(new FormData(e.currentTarget));const items=Object.entries(cart).map(([id,cantidad])=>{const p=products.find(x=>x.id===id);return {id,nombre:p.name,precio:p.price,cantidad}});const pedido={cliente:fd,productos:items,totalEstimado:items.reduce((a,p)=>a+p.precio*p.cantidad,0),origen:'farmaciapanda.com'};const status=$('#orderStatus');status.textContent='Enviando pedido...';try{if(!cfg.apiUrl||cfg.apiUrl.includes('TU-SERVIDOR'))throw new Error('API pendiente');const r=await fetch(cfg.apiUrl,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(pedido)});if(!r.ok)throw new Error('No fue posible enviar');const data=await r.json();status.textContent=`Pedido enviado correctamente. Folio: ${data.folio||data.orderNumber||'registrado'}`;cart={};save()}catch(err){const text=encodeURIComponent(`Pedido Farmacia Panda\nCliente: ${fd.nombre}\nTeléfono: ${fd.telefono}\nDomicilio: ${fd.domicilio}, ${fd.colonia}, ${fd.ciudad}, ${fd.estado}\nProductos:\n${items.map(p=>`- ${p.cantidad} x ${p.nombre} (${money(p.precio)})`).join('\n')}\nTotal estimado: ${money(pedido.totalEstimado)}`);status.innerHTML=`El servidor todavía no está conectado. <a target="_blank" href="https://wa.me/${cfg.whatsapp||'523316012605'}?text=${text}">Enviar ahora por WhatsApp</a>.`}}
categories();render();renderCart();
